// tslint:disable-next-line: no-unused-expression
export class Peer {
  // tslint:disable-next-line: no-unused-expression
  id: string;
  label: string;

  constructor(id: string, label: string) {
    this.id = id;
    this.label = label;
  }
}
